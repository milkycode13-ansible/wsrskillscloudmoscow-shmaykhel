---
# tasks file for ufw

- name: Allow ports
  ufw:
    direction: incoming
    proto: tcp
    port: "{{ item }}"
  loop:
    - 80
    - 8880
    - 1467
- name: Enable and reload UFW
  ufw:
    state: enabled
